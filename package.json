{
  "name": "testact",
  "version": "1.0.0",
  "description": "Apple-style tech landing page",
  "private": true,
  "scripts": {
    "build": "echo 'Build not required for static site'"
  }
}
"scripts": {
  "build": "mkdir -p public && cp index.html public/"
}
